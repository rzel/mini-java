package mini.java.fa;

import static org.junit.Assert.assertTrue;

import java.util.Arrays;
import java.util.Collections;
import java.util.HashSet;

import mini.java.fa.legacy.SimpleFA;
import mini.java.fa.legacy.SimpleFAAdapter;

import org.junit.Test;

/**
 * Test cases for interface DFA and its implementation
 * 
 * @author Alex
 */
public class FiniteAutomatonTest {
    @Test
    // Basic test case for a single transition
    public void testAddTransition() {
//        DFA fa = new SimpleFAAdapter(new SimpleFA());
//
//        State from = new State();
//        State to = new State();
//        Object input = new Object();
//        fa.addTransition(from, to, input);
//
//        // test "to" state
//        assertTrue(fa.getStates().equals(
//                new HashSet<State>(Arrays.asList(from, to))));
//        assertTrue(fa.getStates(from).equals(Collections.singleton(to)));
//        assertTrue(fa.getStates(from, input).equals(Collections.singleton(to)));
//
//        // test "input"
//        assertTrue(fa.getInputs().equals(Collections.singleton(input)));
//        assertTrue(fa.getInputs(from).equals(Collections.singleton(input)));
    }

}
